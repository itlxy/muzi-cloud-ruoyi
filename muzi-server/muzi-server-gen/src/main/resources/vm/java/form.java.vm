package ${packageName}.form;


import io.swagger.annotations.ApiModel;
import com.fasterxml.jackson.annotation.JsonFormat;
import org.springframework.format.annotation.DateTimeFormat;
import io.swagger.annotations.ApiModelProperty;
import lombok.Data;
import ${packageName}.entity.${ClassName}Entity;
import ${basePackage}.common.core.constant.DataValidateGroup;

import javax.validation.constraints.NotBlank;
import javax.validation.constraints.NotNull;
import javax.validation.constraints.PositiveOrZero;
import javax.validation.constraints.Size;
import java.io.Serializable;
import java.time.LocalDateTime;
import java.math.BigDecimal;
import java.util.Date;

/**
 * ${functionName}查询对象
 *
 * @author ${author}
 * @date ${datetime}
 */
@Data
@ApiModel(value = "${functionName}增加，修改修表单对象", description = "${functionName}增加，修改修表单对象")
public class ${ClassName}Form extends ${ClassName}Entity implements Serializable {

}
